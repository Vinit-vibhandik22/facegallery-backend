# Plan: 01-01-PLAN.md — Clustering Core Implementation

**Goal**: Move the clustering logic to the FastAPI backend and switch to average linkage with cosine distance.

**Wave**: 1 (Foundation)
**Depends on**: None
**Requirements**: CLUS-01, CLUS-02, CLUS-03, CLUS-05, INTG-01, INTG-02
**Artifacts**: `python_backend/main.py`, `python_backend/requirements.txt`, `src/lib/clustering.ts`, `src/app/dashboard/projects/[id]/page.tsx`

**must_haves**:
- Single Person = Single Cluster in Dashboard tests.
- Backend handles linkage and metric selection.

## Tasks

### 1. Update Backend Dependencies
**<read_first>**
- `python_backend/requirements.txt`
**<action>**
Add `scikit-learn` and `pandas` to `python_backend/requirements.txt`.
**<acceptance_criteria>**
`python_backend/requirements.txt` contains `scikit-learn`.

---

### 2. Implement Backend `/cluster-faces` Endpoint
**<read_first>**
- `python_backend/main.py`
- `.planning/phases/01-clustering-core/01-RESEARCH.md`
**<action>**
Add an async POST endpoint `/cluster-faces` to `python_backend/main.py` that:
- Accepts a list of embeddings.
- Accepts `epsilon` (threshold), `min_samples`, `linkage` as optional query parameters.
- Uses `sklearn.cluster.AgglomerativeClustering(n_clusters=None, metric='cosine', linkage='average', distance_threshold=epsilon)` on the embeddings.
- Returns labels for each index.
**<acceptance_criteria>**
`python_backend/main.py` contains `@app.post("/cluster-faces")`.
`test_cluster.py` (to be created) returns status code 200.

---

### 3. Transition Next.js Clustering to Backend
**<read_first>**
- `src/lib/clustering.ts`
- `src/app/dashboard/projects/[id]/page.tsx`
**<action>**
Update `src/lib/clustering.ts` to include a new export `clusterFacesRemote(embeddings: number[][], epsilon: number = 0.35)` that:
- POSTs to `process.env.NEXT_PUBLIC_BACKEND_URL + "/cluster-faces"`.
- Returns `ClusterResult` exactly like the existing `dbscan` function.

Update `src/app/dashboard/projects/[id]/page.tsx` (Lines 206, 338) to use `clusterFacesRemote` instead of direct `dbscan`.
**<acceptance_criteria>**
`src/lib/clustering.ts` exports `clusterFacesRemote`.
`src/app/dashboard/projects/[id]/page.tsx` calls `clusterFacesRemote`.

---

### 4. Calibrate the Cosine Threshold
**<read_first>**
- `src/app/dashboard/projects/[id]/page.tsx`
- `.planning/phases/01-clustering-core/01-RESEARCH.md`
**<action>**
Set the default `epsilon` in both the backend and `clusterFacesRemote` call to `0.35`.
Verify this threshold unifies split identities of a test subject without merging distinct people.
**<acceptance_criteria>**
`epsilon` usage is `0.35` in the Next.js page call.

## Verification

### Automated Verification
- [ ] Create `tmp/test_cluster.py` with mock 512D vectors to verify the `/cluster-faces` endpoint logic.
- [ ] Backend server runs locally or on remote with the new endpoint.

### Success Criteria (Must be TRUE)
- [ ] Backend uses `average` linkage and `cosine` distance.
- [ ] Fragmentation for a variation-heavy person is significantly reduced compared to `Complete` linkage.
- [ ] The dashboard correctly displays face groupings after the backend clustering completes.
