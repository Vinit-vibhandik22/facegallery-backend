# Plan: 03-01-PLAN.md — Validation & Polish Implementation

**Goal**: Finalize verification of accurate one-shot face clustering and clean up the environment.

**Wave**: 1 (Final Verification)
**Depends on**: Phase 2
**Requirements**: VERI-01, VERI-02
**Artifacts**: `tmp/final_accuracy_audit.py`, `PROJECT.md`

**must_haves**:
- Accuracy is verified with split-preventing tests.
- Backend is stable.
- Dashboard properly reflects unified clusters.

## Tasks

### 1. Final Accuracy Audit
**<read_first>**
- `tmp/test_refinement.py`
- `python_backend/main.py`
**<action>**
Create `tmp/final_accuracy_audit.py` that:
1. Simulates TWO people.
2. Person 1 has 3 embeddings (Front, Side, Tilted - forming a "chain" that Complete Linkage usually splits).
3. Person 2 has 2 frontal embeddings.
4. Verify that the NEW backend clusters them into exactly TWO clusters.
**<acceptance_criteria>**
`tmp/final_accuracy_audit.py` returns `Labels: [0, 0, 0, 1, 1]`.

---

### 2. Manual UI Verification
**<read_first>**
- `src/app/dashboard/projects/[id]/page.tsx`
**<action>**
1. Run the Next.js app (`npm run dev`) and the Python backend concurrently.
2. In the dashboard, select a project and run "Re-sort All Photos".
3. Visually verify the resulting cluster structure for a known identity.
**<acceptance_criteria>**
The "Re-sort" results are consistent with the improved accuracy (fewer split clusters).

---

### 3. Final Polish and Cleanup
**<read_first>**
- `PROJECT.md`
**<action>**
1. Remove all `tmp/*.py` and `python_backend/*.py` test scripts created during the upgrade.
2. Finalize `PROJECT.md` to reflect the completed Phase 1-3 upgrade.
**<acceptance_criteria>**
`PROJECT.md` status is "Complete". `tmp/` and test files are removed.

## Verification

### Automated Verification
- [ ] `tmp/final_accuracy_audit.py` PASSES with exactly 2 clusters for 5 vectors (3+2).

### Success Criteria (Must be TRUE)
- [ ] Accuracy upgrade (Average Linkage + Cosine + Centroid Merge) is verified to prevent identity splitting.
- [ ] Final codebase is clean of test artifacts.
